name: Build Mitmproxy (macOS 12 x86_64)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest # Uses the standard free Apple runner

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Build Tools
        run: python -m pip install --upgrade pip build

      - name: Build Source and Wheel
        env:
          MACOSX_DEPLOYMENT_TARGET: "12.0" # Targets Monterey
          ARCHFLAGS: "-arch x86_64" # Forces Intel architecture
        run: python -m build

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mitmproxy-macos12-wheel
          path: dist/*.whl
