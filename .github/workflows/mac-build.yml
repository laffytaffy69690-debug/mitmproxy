name: Build macOS executable

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build-macos:
    name: Build macOS executable (PyInstaller)
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'   # <-- use Python 3.12 or later

      - name: Install Homebrew packages
        run: |
          brew update
          brew install openssl pkg-config || true

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python build dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pyinstaller
          # Install the package so entrypoints/modules are available
          pip install .

      - name: Build macOS binary with PyInstaller
        run: |
          rm -rf build dist || true
          # Adjust entrypoint if needed (e.g. mitmproxy, mitmdump, mitmweb)
          python -m PyInstaller --onefile --name mitmproxy mitmproxy/__main__.py
          # Example additional targets (uncomment if needed):
          # python -m PyInstaller --onefile --name mitmdump mitmproxy/tools/mitmdump.py
          # python -m PyInstaller --onefile --name mitmweb mitmproxy/tools/mitmweb.py

      - name: List built artifacts
        run: |
          ls -lh dist || true

      - name: Upload macOS binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: mitmproxy-macos
          path: dist/mitmproxy
